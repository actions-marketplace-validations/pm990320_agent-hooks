name: service-b
steps:
  lint:
    run: bash scripts/lint.sh {files}
    files: "src/**/*.txt"
pipelines:
  ci:
    steps: [lint]
  pre-commit:
    steps: [lint]
git:
  hooks:
    pre-commit:
      pipeline: pre-commit
